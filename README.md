
  # Ausleihsystem

  This is a code bundle for Ausleihsystem. The original project is available at https://www.figma.com/design/AL6EbF4FDrqTvSPfCUTtOe/Ausleihsystem.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  